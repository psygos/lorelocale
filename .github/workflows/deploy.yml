name: Deploy to Cloudflare Pages (disabled)

on:
  workflow_dispatch:

# Cloudflare deployment temporarily disabled.
jobs:
  deploy:
    if: false  # prevent execution
    runs-on: ubuntu-latest
    steps: [] 